根据输入的内容，返回一个JSON数组。内容如果包含中文，请一律转换为<%= lang %>！

主题：<%= topic %>
内容：<%= text %>


阅读内容，输出Tags列表，但是要考虑与主题相关性，如果与主题有关，则Tag数量至少3个，最多5个。输出格式：
{
    "Type": "tag1",
    "Tags": [
        "tag2",
        "tag3"
    ]
}

注意：如果内容与主题完全无关，就直接返回一个空的JSON！